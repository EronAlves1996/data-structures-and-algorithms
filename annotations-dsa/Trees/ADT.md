1. Position
	* getElement()
2. Tree
	* root()
	* parent(p)
	* children(p)
	* numChildren(p)
	* isInternal(p) -> has at least one child
	* isExternal(p) -> has no child
	* isRoot(p)
	* size()
	* isEmpty()
	* iterator()
	* positions()